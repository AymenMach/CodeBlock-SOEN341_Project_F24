/* Container for the entire page */
.list-of-students-container {
  padding: 20px;
  max-width: 1200px;
  margin: 20px auto;
  background-color: #f9f9f9;
  border-radius: 10px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

/* Page Header */
.list-of-students-container h1 {
  text-align: center;
  font-size: 28px;
  color: #00509e;
  margin-bottom: 20px;
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
}

/* Styling for buttons */
button.custom-button {
  background-color: #00509e;
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  margin-right: 10px;
  transition: background-color 0.3s ease, transform 0.2s ease;
  font-size: 16px;
}

button.custom-button:hover {
  background-color: #003d80;
  transform: scale(1.05);
}

button.custom-button:active {
  background-color: #002b5c;
  transform: scale(0.95);
}

/* Dropdown styles */
select.custom-dropdown {
  padding: 10px;
  font-size: 16px;
  border: 1px solid #ddd;
  border-radius: 5px;
  margin-right: 10px;
  cursor: pointer;
  background-color: #ffffff;
  color: #333;
  transition: border-color 0.3s ease;
}

select.custom-dropdown:focus {
  border-color: #00509e;
  outline: none;
}

/* Styling for the table */
table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 20px;
  background-color: #ffffff;
  border-radius: 5px;
  overflow: hidden;
}

th, td {
  padding: 12px;
  text-align: left;
  border-bottom: 1px solid #ddd;
  font-size: 16px;
  color: #333;
}

th {
  background-color: #00509e;
  color: white;
  font-weight: bold;
}

tr:hover {
  background-color: #f1f8ff;
}

/* Message styling for empty state */
.list-of-students-container p {
  text-align: center;
  font-size: 18px;
  color: #666;
  margin-top: 20px;
}
